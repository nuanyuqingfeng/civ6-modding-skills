# 实装记录：本次 UI/GP 范围验证结果写入 civ6-modding skill

> 执行时间：2026-09-08 17:04:27　|　skill 路径：`%USERPROFILE%\.agents\skills\civ6-modding`
> 数据来源：本次 FireTuner 全量扫描（4857 行）+ 预分类审核清单 + `Civ6LuaHelper.exe` 内置 `humanChecked`（18 条）

## 1. 标记规则（按你的指定）

| verify_status | 判定依据 | 行数 |
|---|---|---:|
| `已核验` | 本次核查过关（判定「一致」）+ P1（已实装修正 / 已核出真身） | 3256 |
| `存疑` | **仅**本次查出 API 文档自身不对者（availability 标错 / 名称路径错 / 运行时确无此名 / CodeBuddy 文档转储） | 1434 |
| `''` 留空 | **本次无法实测**（缺实例通道或事件动态代理）——不挂已核验、也不挂存疑 | 167 |
| 合计 | | 4857 |

**未删除任何条目**；P1 之外的嫌疑行只标记、保留原值。

## 2. api.sqlite 变更

新增 11 列：`verify_status` `verify_scope` `verify_note` `verify_at` `runtime_gp` `runtime_ui` `audit_priority` `suspect_type` `corrected_from` `true_name` `true_path`

- **P1 实装修正 availability：53 条**
  - `Both` → `GamePlay`：28 条
  - `UI` → `Both`：20 条
  - `Both` → `UI`：5 条
- 注真身：`true_name` 45 条、`true_path` 7 条（含三层压平 `Unit:SetMilitaryFormation()`、`ImprovementBuilder` 命名空间、`SetScoringScenario1/2/3`、`GetAllianceLevel` 等）
- 审核优先级分布：—=3196，转储=1190，P4=191，待补测=167，P1=60，P2=53
- 存疑构成：文档转储（非运行时命名空间）=1190，A availability 标错=134，C 运行时确无此名=109，B 文档名可修=1

## 3. reference/api_enhanced.json 变更（助手「人工核查」字段）

| 字段 | 条数 | 说明 |
|---|---:|---|
| `humanChecked: true` | 3214 | 本次核查过关的全部 API（含 P1 已修正），并追加 `[核验]` 备注行 |
| `suspect: true` | 76 | 文档自身有误，追加 `[存疑]` 备注行（未删除） |
| `pendingTest: true` | 143 | 无法实测：**不挂核验/存疑标记、不加备注行**，仅记 `pendingReason` |
| `availabilityCorrectedFrom` | 53 | P1 修正前的原值留痕 |
| `trueName` / `truePath` | 44 / 7 | 运行时真名 / 真身路径 |
| `helperHumanChecked` | 18 | Civ6LuaHelper 原有 18 条人工验证已并入（另存 `helperAvailability`） |

附带：`metadata.verification` 记录方法、日期、字段语义与计数；文件保持原有 UTF-8 无 BOM / LF / indent=2（round-trip 校验通过）。

## 4. 文档与工具同步

- `SKILL.md`：二问查询块加核验查询、「铁律」后加**核验优先**提示、数据查询表加 4 行、数据库文件表更新，并新增小节「API 核验字段（2026-09-08 FireTuner 全量实测 · UI/GP 范围存在性）」（三态规则表、列语义、JSON 字段、冲突提示、报告位置）。保持 CRLF。
- `database/scripts/query_api.py`：列表与 `--object` 增加「核验」列；`--show` 增加 Verification 区块；新增互斥过滤 `--verified` / `--suspect` / `--pending`。保持 LF。

## 5. 备份处理

- 已按要求**删除** skill 内既有备份：`database/api.sqlite.bak_pre_tuner_20260908_112150`（该备份与当时 api.sqlite 仅差 1 行：`Q-PlayerVisibilityManagerGetPlayerVisibility` 的 availability UI→Both，此改动与本次实测一致，已保留并在 verify_note 留痕）。
- skill 内**未新增任何备份文件**；变更前的原始副本存放在 skill 之外：`%USERPROFILE%\AppData\Local\Temp\civ6_api_scope\out\pre_install`（`api.sqlite.orig` / `api_enhanced.json.orig`），需要回滚时直接覆盖即可。
- 旁注：`database/q_sle.py` 是早先留下的一次性查询脚本（非备份文件），未动。

## 6. 需你裁决的 1 条

`City:GetBuildQueue():GetTurnsLeft()`（id `Q-CityGetBuildQueueGetTurnsLeft`）：助手人工验证结论 availability=UI，本次实测 GP/UI 两端都存在该函数（GP=function、UI=function）。已按实测保留 `Both`，并把人工结论写入 `verify_note`、JSON 里记 `helperAvailability='UI'`。若严格以人工结论为准，一条 SQL 改回：

```sql
UPDATE api_functions SET availability='UI', corrected_from='Both',
  verify_note=verify_note||'；已按人工验证结论改回 UI'
 WHERE id='Q-CityGetBuildQueueGetTurnsLeft';
```

## 7. 复核命令

```powershell
python database/scripts/query_api.py --search Key --verified   # 只看已核验
python database/scripts/query_api.py --search Key --suspect    # 只看存疑（文档自身有误）
python database/scripts/query_api.py --search Key --pending    # 只看待补测（标记留空）
python database/scripts/query_api.py --show City.GetTurnsLeft  # 详情含 Verification 区块
```

